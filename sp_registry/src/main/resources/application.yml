server:
  port: 10086
spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
#   不注册自己到注册中心
    register-with-eureka: false
#   不拉取自己
    fetch-registry: false

